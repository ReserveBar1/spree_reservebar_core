<% if controller.controller_name == "orders" && controller.action_name == "show" %>
  <% if flash[:commerce_tracking] %>
    <% 
      quantities = []
      product_names =[]
      @order.line_items.each do |li|
        if li.product.taxons.map(&:name).include? 'Patrón'
          quantities << li.quantity
          product_names << li.product.name.gsub(' ','+').gsub("'","")
        end
      end
      quantities = quantities.sum
      product_names = product_names.join('|')
    %>
    <% unless product_names.empty? %>
      <script>document.write('<s'+'cript language="JavaScript" src="http://jact.atdmt.com/jaction/Post_AgeVer_ReserveBar_OrderConf"></s'+'cript>')</script><noscript><iframe src="http://iact.atdmt.com/iaction/Post_AgeVer_ReserveBar_OrderConf" width="1" height="1" frameborder="0" scrolling="No" marginheight="0" marginwidth="0" topmargin="0" leftmargin="0"></iframe></noscript>
    <% end %>
  <% end %>
<% end %>
